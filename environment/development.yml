# -- Databases -----------------------------------------------------------------
mongo:
  - name  : primary
    host  : 127.0.0.1
    port  : 27017
    db    : where

# -- Authentication ------------------------------------------------------------

# -- Databases -----------------------------------------------------------------
